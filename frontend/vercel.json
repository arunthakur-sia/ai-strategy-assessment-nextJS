{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "VITE_API_URL": "https://authentic-reverence-production-45b9.up.railway.app"
  },
  "envComment": "VITE_API_URL should be the backend hostname (e.g. https://something.railway.app), NOT including /api suffix. The frontend will automatically append /api.",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "X-XSS-Protection", "value": "1; mode=block" }
      ]
    }
  ]
}
